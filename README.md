# todo app

# build steps

npm create vite@latest
npm i
npm i -D vitest jsdom
npm i react router
copy vscode folder
copy .prettierrc.json
add test to vite.config.js

```
  test: {
    environment: 'jsdom',
  },
```

init git
